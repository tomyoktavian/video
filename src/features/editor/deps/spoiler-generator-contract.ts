/**
 * Cross-feature contract — Spoiler Generator components and constants
 * used by the Editor feature (settings dialog + lazy-loaded dialog mount).
 */

export { DEFAULT_SCRIPT_WRITER_SYSTEM_PROMPT } from '@/features/spoiler-generator/system-prompt'
export { SpoilerGeneratorDialog, RegenerateNarrationDialog } from '@/features/spoiler-generator'
