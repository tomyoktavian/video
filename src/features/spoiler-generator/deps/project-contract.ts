/**
 * Cross-feature contract — Project store. Used by Spoiler Generator to
 * resolve the active project id (required for media library imports).
 */

export { useProjectStore } from '@/features/projects'
