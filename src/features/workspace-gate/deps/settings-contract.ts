/**
 * Adapter exports for settings dependencies used by workspace-gate.
 * Workspace-gate modules must import settings stores through this file.
 */

export { hydrateCustomAiStore } from '@/features/settings'
